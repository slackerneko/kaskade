```js
import kd from 'kaskade';
import AnotherComponent from './AnotherComponent';

const styles = kd.create(CurrentComponent, ({
  _this, // current component slector
  parent, // parent context slector
  children, // all known children selector
  variables, // token accessor with scope-aware autocomplete
  comp // function to reference another react component
}) => ({
  [_this]: {
    backgroundColor: 'grey',
    '&:hover': {
      backgroundColor: 'lightgrey',
    }
  },
  [parent]: {
    backgroundColor: 'darkgrey'
  },
  [children]: {
    backgroundColor: 'lightgrey'
  },
  [comp(AnotherComponent)]: {
    backgroundColor: 'lightgrey'
  }
}))
```
