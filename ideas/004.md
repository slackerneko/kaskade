```js
import kd from 'kaskade';
import { A, B } from './components/index.ts';

const styles = kd.create([
  {
    this: {
      backgroundColor: 'grey'
    },
    parent: {
      component: [A, B],
      display: 'flex',
      flexDirection: 'column',
    }
  }
])
```
